sudo: false
language: python

os:
 - linux

python:
  - "2.7"

global:
  env:
    - COMMIT_AUTHOR_EMAIL: "f.zapata@esciencecenter.nl"

install:
  # install library
  - pip install numpy scipy --user 
  - pip install -e .[test]

script:
  - (cd tests && converage run -m unittest discover -vvv)

branches:
  only:
    - master
